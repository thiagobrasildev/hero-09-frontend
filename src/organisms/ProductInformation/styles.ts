import styled from "styled-components";

export const Container = styled.div`
  h1 {
    color: #1255ee;
    font-size: 56px;
    font-weight: bold;
  }

  h6 {
    font-size: 16px;
    color: rgba(30, 37, 94, 0.7);
    font-weight: 400;
    padding-right: 150px;
    margin-top: 25px;
  }
`;
